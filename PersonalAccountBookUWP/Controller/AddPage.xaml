<Page
    x:Class="PersonalAccountBookUWP.Controller.AddPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PersonalAccountBookUWP.Controller"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" Name="AccountChooseBox" Header="계좌 선택" PlaceholderText="계좌를 선택하세요." Margin="10,10,10,10" Width="380"/>
            <TextBlock Grid.Column="1" Name="BeforeBalanceTextBlock" Text="계산 전 잔액이 표시됩니다." Margin="20,45,10,10"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" Name="IncOrDecComboBox" Header="금액 증감" PlaceholderText="+ 또는 -" Margin="10,10,10,10" Width="100"/>
            <ComboBox Grid.Column="1" Name="CurrencyComboBox" Header="통화" PlaceholderText="\ 등" Margin="10,10,10,10" Width="100"/>
            <TextBox Grid.Column="2" Name="AmountTextBox" Header="거래 금액" PlaceholderText="금액을 입력하세요. (숫자만)" Margin="10,10,10,10" IsSpellCheckEnabled="True"/>
            <TextBlock Grid.Column="3" Name="AfterBalanceTextBlock" Text="계산 후 잔액이 표시됩니다." Margin="20,45,10,10"/>
        </Grid>
        <ComboBox Grid.Row="2" Name="TransactionTypeComboBox" Header="거래 유형" PlaceholderText="거래 유형을 선택하세요." Margin="10,10,10,10" Width="210"/>
        <TextBox Grid.Row="3" Name="BankBookTextBox" Header="거래 대상" PlaceholderText="통장내역에 있는 거래 대상을 입력하세요." Margin="10,10,10,10" IsSpellCheckEnabled="True" Width="400" HorizontalAlignment="Left"/>
        <TextBox Grid.Row="4" Name="CardBookTextBox" Header="자세한 거래 대상" PlaceholderText="(옵션) 카드내역에 있는 거래 대상을 입력하세요." Margin="10,10,10,10" IsSpellCheckEnabled="True" Width="400" HorizontalAlignment="Left"/>
        <TextBox Grid.Row="5" Name="ContentTextBox" Header="내용" PlaceholderText="(옵션)카드내역에 있는 거래 대상을 입력하세요" Margin="10,10,10,10" TextWrapping="Wrap"
                 IsSpellCheckEnabled="True" HorizontalAlignment="Stretch" IsTextPredictionEnabled="True" AcceptsReturn="True"/>
        <CommandBar Grid.Row="6">
            <AppBarButton Name="EraseButton"  Label="지우기" Click="EraseButton_Click">
                <AppBarButton.Icon>
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xEF17;"/>
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton Name="SaveButton" Label="저장" Click="SaveButton_Click">
                <AppBarButton.Icon>
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE105;" />
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>
    </Grid>
</Page>
